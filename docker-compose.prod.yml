services:
  backend:
    build:
      context: .
      dockerfile: ./be_src/Dockerfile.prod
    ports:
      - "8000:8000"
    restart: unless-stopped
  
  frontend:
    build:
      context: ./fe_src
      dockerfile: Dockerfile.prod
    ports:
      - "5173:5173"
    restart: unless-stopped

